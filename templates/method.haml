.panel.panel-primary.method-block{id: @id}
  .panel-heading
    .panel-title
      %span.signature!= @signature
      %a{href: "##{@id}"}
        %span.octicon.octicon-link
      %a{href: @srcUrl}
        %span.octicon.octicon-file-code
  .panel-body
    != @description
    != @examples
    != @parameterBlock
    != @returnValueBlock
